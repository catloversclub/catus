meta {
  name: Get Cat Image Upload URL (Step 1)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/cat/:id/image/upload-url
  body: json
  auth: inherit
}

params:path {
  id:
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "contentType": "image/png"
  }
}

vars:post-response {
  : 
  : 
  : 
}

script:post-response {
  await require("./presigned-url").savePresignEnv(res);
}
